@model IEnumerable<IpharmWebAppProject.Models.ProductInWishList>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WishListID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WishListID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Product.ProductID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProductInWishListID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProductInWishListID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProductInWishListID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
